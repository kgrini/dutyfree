
    <table>
      <%= render(cart.line_items) %>
      <tr class="total_line" >
        <td colspan="2" >Total</td>
        <td class="total_cell" ><%= cart.total_price %></td>
      </tr>
    </table>

    <%= button_to 'Очистить товары?' , cart, :method => :delete,
                  :confirm => 'Are you sure?' %>
    <%= button_to 'Открыть форму заказа' %>

